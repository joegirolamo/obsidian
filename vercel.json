{
  "rewrites": [
    { "source": "/(.*)", "destination": "/apps/connect/$1" }
  ],
  "buildCommand": "chmod +x build.sh && ./build.sh",
  "outputDirectory": "apps/connect/.next",
  "framework": "nextjs",
  "git": {
    "deploymentEnabled": {
      "main": true
    }
  }
} 